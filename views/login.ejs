<!DOCTYPE html>
<html>
  <head>
    <title>Registr</title>
    <link rel='stylesheet' href='/stylesheets/login.css' />
    <link rel='stylesheet' href='/stylesheets/header.css' />
  </head>
  <body>
    <%- include('./parts/header.ejs')  %>
    <div class="wrapper">
      <div class="box-btn">
        <a href="" class="btn__a" data-choice="Registr">Registr</a>
        <a href="" class="btn__a" data-choice="login">Login</a>
      </div>
      <h1></h1> 
      <% if (errors?.length > 0) { %>
         <ul class="error-box">
           <% errors?.forEach(function(error) { %>
             <li class="error-message"><%= error?.msg %></li>
           <% }); %>
         </ul>
         <% } %>
         <% if (messages.length > 0) { %>
            <ul class="message-err">
              <% messages.forEach(function(message) { %>
                <li><%= message %></li>
             <% }); %>
            </ul>
         <% } %>
  <!-- <form class="" action="" method="post">
    <label id="user-name" for="">Enter your Name:
      <input  name="user" type="text" placeholder="Your Name" value="<%= user?.user%>">
    </label>
    <label for="">Enter your E-mail:
      <input  name="userName" type="email" placeholder="Your E-mail" value="<%= user?.userName %>" required>
    </label>
    <label for="" >Enter your Password:
      <input  name="password" type="password" placeholder="Your Password" value="<%= user?.password %>" required>
    </label>
      <button class="btn" id="btn-rl" type="submit"></button>
  </form> -->
  </div>
  <script src="/javascripts/login.js"></script>
  </body>
</html>
